export enum USER_ROLE {
  USER = 'user',
  ADMIN = 'admin',
  HR = 'hr',
  MANAGER = 'manager'
}

export enum USER_STATUS {
  UNVERIFIED = 'unverified',
  VERIFIED = 'verified',
  BLOCKED = 'blocked',
  DELETED = 'deleted'
}
